$('#feed').append("<%= j render @tweets %>");

<% if @tweets.next_page %>
  $(".pagination").replaceWith("<%= j will_paginate(@tweets) %>");
  $(".pagination").hide();
<% else %>
  $(window).off('scroll');
  $("#loader").remove();
  $(".pagination").remove();
<% end %>