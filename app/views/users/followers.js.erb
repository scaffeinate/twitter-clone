$('#feed').append("<%= j render @followers %>");

<% if @followers.next_page %>
  $(".pagination").replaceWith("<%= j will_paginate(@followers) %>");
  $(".pagination").hide();
<% else %>
  $(window).off('scroll');
  $("#loader").remove();
  $(".pagination").remove();
<% end %>