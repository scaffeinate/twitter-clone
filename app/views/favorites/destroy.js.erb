<% if @favorite.destroy %>
  $("#favor-tweet-<%= @favorite.tweet_id %>").html("<%=j render('favorites/favorite_button', tweet_id: @favorite.tweet_id) %>");
<% else %>
  $('#message').html("<%=j render 'partials/messages', message: 'Something went wrong there. Try again?' %>");
<% end %>