<% if @favorite.save %>
  $("#favor-tweet-<%= @favorite.tweet_id %>").html("<%=j render('favorites/favorite_button', tweet_id: @favorite.tweet_id) %>");
<% else %>
  $('#message').html("<%=j render 'partials/messages', message: 'Couldn\'t favour tweet. Try again?' %>");
<% end %>