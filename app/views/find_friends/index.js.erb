$('#find_friends').append("<%= j render @users %>");

<% if @users.next_page %>
  $(".pagination").replaceWith("<%= j will_paginate(@users) %>");
  $(".pagination").hide();
<% else %>
  $(window).off('scroll');
  $("#loader").remove();
  $(".pagination").remove();
<% end %>